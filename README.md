A simple ol' number visualizer to help show how good a random number generator is. Using raylib and imgui for visuals and a tool to change values for the randomizer.

Note: I realized that the randomWithSeed option only populates the matrix with the same value. I did not add another algorithm like with the szudzik algorithm. Effectively using randomWithSeed without hashing is useless. tehe
